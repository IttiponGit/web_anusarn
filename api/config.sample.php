<?php

/**
 * ตัวอย่างไฟล์ config สำหรับ Hosting (Production)
 * 
 * การใช้งาน:
 * 1. คัดลอกไฟล์นี้และตั้งชื่อเป็น config.php ในโฟลเดอร์เดียวกัน
 * 2. แก้ไขค่าตามข้อมูลการเชื่อมต่อจริง
 * 3. ไฟล์ config.php จะถูก ignore โดย .gitignore
 * 
 * *** ห้ามปล่อย config.php ลงไปใน GitHub/Git ***
 */

// ข้อมูลเซิร์ฟเวอร์ฐานข้อมูล
$host = 'your_database_host';        // เช่น: db.example.com, 192.168.1.1 เป็นต้น
$port = 3306;                        // พอร์ตฐานข้อมูล (ค่าปกติ: 3306)
$dbname = 'your_database_name';      // ชื่อฐานข้อมูล
$username = 'your_db_username';      // ชื่อผู้ใช้ฐานข้อมูล
$password = 'your_db_password';      // รหัสผ่านฐานข้อมูล

/**
 * บันทึกเพิ่มเติมสำหรับ Hosting:
 * - สอบถามที่บริการ Hosting เพื่อขอข้อมูล host, port, dbname, username, password
 * - ใช้ฐานข้อมูลที่แข็งแกร่ง (อย่างน้อย 12 ตัวอักษร มีตัวพิมพ์ใหญ่, เล็ก, ตัวเลข, อักษรพิเศษ)
 * - หลังจากตั้งค่าเรียบร้อย ให้รันสคริปต์ schema.sql และ seed.sql เพื่อสร้างตารางและข้อมูล
 */
